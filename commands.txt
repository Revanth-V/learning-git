git config --global user.name
git config --global user.email
// learn what does these signify:
// .
// cd
// cd ..
// mkdir
// echo.>
// dir
// notepad
// type
// rmdir
git init .
git add <filename>
git add .
git add -A
git commit -m <message>
git status
git log
git diff
git show <hash_value>
git commit --amend -m <message>

// push an existing repository from the command line
git remote add origin https://github.com/Revanth-V/learning-git.git
git branch -M main
git push -u origin main
